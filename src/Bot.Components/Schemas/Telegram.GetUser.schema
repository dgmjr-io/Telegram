{
  "$schema": "https://schemas.botframework.com/schemas/component/v1.0/component.schema",
  "$role": "extends(Telegram.TelegramBotCustomAction)",
  "type": "object",
  "title": "Get User",
  "description": "Retrieves a user's information via the Telegram Bot API.",
  "required": [
    "recipientId",
    "botApiToken"
  ]
}
